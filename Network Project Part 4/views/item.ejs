<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Item Detail</title>
    <link rel="stylesheet" href="/styles/styles.css">
</head>
<body>
    <%- include('./partials/header.ejs')%>

    <div class="item-detail">
        <img src="<%= item.image %>" alt="<%= item.title %>"> 
        <div class="details">
            <h2><%= item.title %> (<%= item.details %>)</h2>
            <p>Condition: <%= item.condition %></p>
            <p>Price: $<%= item.price %></p>
            <p>Seller: <%= item.seller %></p>
            <p>ID: <%= item._id %></p>
            <p>Active: <%= item.active %></p>

            <p>Total Offers: <%= item.totalOffers %></p>

            <% if(highestOffer) { %>
                <p>Highest Offer: $<%= highestOffer.amount %></p>
            <% } else { %>
                <p>No highest offer yet</p>
            <% } %>

            <% if(item.active) { %>
                <form action="/items/<%= item._id %>/offers" method="POST">
                    <label for="offerAmount">Your Offer:</label>
                    <input type="number" id="offerAmount" name="offerAmount" min="0.01" step="0.01" required>
                    <button type="submit">Make an Offer</button>
                </form>
            <% } else { %>
                <p>Unfortunately, this item is no longer available.</p>
            <% } %>
        </div>
    </div>

    <%- include('./partials/footer.ejs') %>
</body>
</html>